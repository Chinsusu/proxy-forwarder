diff --git a/.github/workflows/release-tag.yml b/.github/workflows/release-tag.yml
new file mode 100644
index 0000000..1234567
--- /dev/null
+++ b/.github/workflows/release-tag.yml
@@ -0,0 +1,70 @@
+name: Release & Tag
+
+on:
+  # Manual trigger - você pode entrar a versão (opcional)
+  workflow_dispatch:
+    inputs:
+      version:
+        description: 'Version (ex: 0.3.2). Deixe vazio para auto-bump patch.'
+        required: false
+  # Auto trigger - push para main com [release] na mensagem
+  push:
+    branches: [ main, master ]
+
+permissions:
+  contents: write   # para criar tag/release
+
+jobs:
+  release:
+    if: ${{ github.event_name == 'workflow_dispatch' || contains(github.event.head_commit.message, '[release]') }}
+    runs-on: windows-latest
+    env:
+      APP_CSProj: src/ProxyForwarder.App/ProxyForwarder.App.csproj
+    steps:
+      - name: Checkout
+        uses: actions/checkout@v4
+        with:
+          fetch-depth: 0  # Precisa do histórico para ler tags antigas
+
+      - name: Determine version
+        id: ver
+        shell: bash
+        run: |
+          INPUT_VER="${{ github.event.inputs.version }}"
+          if [[ -n "$INPUT_VER" ]]; then
+            V="$INPUT_VER"
+          else
+            LAST=$(git describe --tags --abbrev=0 2>/dev/null | sed 's/^v//')
+            if [[ -z "$LAST" ]]; then LAST="0.1.0"; fi
+            IFS='.' read -r MA MI PA <<< "$LAST"
+            PA=$((PA+1))
+            V="${MA}.${MI}.${PA}"
+          fi
+          echo "version=$V" >> $GITHUB_OUTPUT
+          echo "Resolved version: $V"
+
+      - name: Setup .NET
+        uses: actions/setup-dotnet@v4
+        with:
+          dotnet-version: '8.0.x'
+
+      - name: Publish (win-x64, self-contained)
+        shell: pwsh
+        run: |
+          dotnet publish $env:APP_CSProj -c Release -r win-x64 `
+            --self-contained true `
+            /p:Version=${{ steps.ver.outputs.version }} `
+            -o out/win-x64
+          Compress-Archive -Path out/win-x64/* -DestinationPath out/ProxyForwarder-win-x64-v${{ steps.ver.outputs.version }}.zip
+
+      - name: Create GitHub Release
+        uses: softprops/action-gh-release@v2
+        with:
+          tag_name: v${{ steps.ver.outputs.version }}
+          name: Proxy Forwarder v${{ steps.ver.outputs.version }}
+          files: |
+            out/ProxyForwarder-win-x64-v${{ steps.ver.outputs.version }}.zip
+          generate_release_notes: true
+
+      - name: Done
+        run: echo "Released v${{ steps.ver.outputs.version }}"
diff --git a/Directory.Build.props b/Directory.Build.props
index 1234567..abcdefg 100644
--- a/Directory.Build.props
+++ b/Directory.Build.props
@@ -1,6 +1,12 @@
 <Project>
   <PropertyGroup>
+    <!-- Version Configuration -->
+    <Version>0.1.0</Version>
+    <AssemblyVersion>$(Version)</AssemblyVersion>
+    <FileVersion>$(Version)</FileVersion>
+    <InformationalVersion>$(Version)</InformationalVersion>
+    <RepositoryUrl>https://github.com/yourusername/proxy-forwarder-skeleton</RepositoryUrl>
+
+    <!-- Code Quality -->
     <Nullable>enable</Nullable>
     <ImplicitUsings>enable</ImplicitUsings>
     <TreatWarningsAsErrors>false</TreatWarningsAsErrors>
diff --git a/src/ProxyForwarder.App/MainWindow.xaml.cs b/src/ProxyForwarder.App/MainWindow.xaml.cs
index 1234567..abcdefg 100644
--- a/src/ProxyForwarder.App/MainWindow.xaml.cs
+++ b/src/ProxyForwarder.App/MainWindow.xaml.cs
@@ -1,6 +1,7 @@
 // <copyright file="MainWindow.xaml.cs" company="ProxyForwarder">
 // Copyright (c) ProxyForwarder. All rights reserved.
 // </copyright>
+using System.Reflection;
 
 using System.Windows;
 
@@ -10,5 +11,20 @@ namespace ProxyForwarder.App;
 {
     public MainWindow()
     {
         InitializeComponent();
+        DisplayVersion();
+    }
+
+    private void DisplayVersion()
+    {
+        var version = Assembly.GetExecutingAssembly()?
+            .GetCustomAttribute<AssemblyInformationalVersionAttribute>()?
+            .InformationalVersion ?? Assembly.GetExecutingAssembly().GetName().Version?.ToString() ?? "unknown";
+        
+        if (!string.IsNullOrWhiteSpace(version) && !version.EndsWith("unknown"))
+        {
+            this.Title = $"{this.Title}  v{version}";
+        }
     }
 }
\ No newline at end of file
