<UserControl x:Class="ProxyForwarder.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20" Width="700">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="API Base URL"/>
        <TextBox Grid.Row="1" Text="{Binding ApiBaseUrl}"/>
        <TextBlock Grid.Row="2" Text="Port Range (min - max)" Margin="0,8,0,0"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <TextBox Width="120" Text="{Binding PortRangeMin}" Margin="0,0,8,0"/>
            <TextBlock Text="→" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Width="120" Text="{Binding PortRangeMax}"/>
        </StackPanel>
        <CheckBox Grid.Row="4" Content="Auto start forwarders on launch" IsChecked="{Binding AutoStartForwarders}" Margin="0,8,0,0"/>
        <Button Grid.Row="5" Content="Save" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButton}" Margin="0,12,0,0" Width="120"/>
    </Grid>
</UserControl>
