<UserControl x:Class="ProxyForwarder.App.Views.ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="24">
        <TextBlock Text="CloudMini API Token" FontWeight="SemiBold"/>
        <PasswordBox x:Name="TokenBox" PasswordChar="•" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,0,0,20"/>
        <Button Height="48" Content="Sync Proxy" Command="{Binding SyncAllCommand}" Style="{StaticResource PrimaryButton}"/>
        <TextBlock Text="{Binding SyncMessage}" Margin="0,12,0,0" Foreground="{StaticResource Brush.Text}" TextWrapping="Wrap"/>
    </StackPanel>
</UserControl>
