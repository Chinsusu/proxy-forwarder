<UserControl x:Class="ProxyForwarder.App.Views.ProxiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="10">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,6">
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource PrimaryButton}" Height="36"/>
        </StackPanel>
        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" HeadersVisibility="Column"
                  CanUserAddRows="False" Background="{StaticResource Brush.Surface}" 
                  Foreground="{StaticResource Brush.Text}" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="120"/>
                <DataGridTextColumn Header="Host" Binding="{Binding Host}" Width="200"/>
                <DataGridTextColumn Header="Port" Binding="{Binding Port}" Width="80"/>
                <DataGridTextColumn Header="User" Binding="{Binding Username}" Width="120"/>
                <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="100"/>
                <DataGridTextColumn Header="Expiration" Binding="{Binding ExpirationDate, StringFormat='yyyy-MM-dd HH:mm'}" Width="150"/>
                <DataGridTextColumn Header="Th.gian hết hạn" Binding="{Binding ExpirationDate}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
