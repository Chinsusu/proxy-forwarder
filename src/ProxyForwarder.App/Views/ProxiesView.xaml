<UserControl x:Class="ProxyForwarder.App.Views.ProxiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="10">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,6" Height="32">
            <TextBlock Text="Type:" VerticalAlignment="Center" Margin="0,0,6,0" Foreground="{StaticResource Brush.Text}"/>
            <ComboBox ItemsSource="{Binding Types}" SelectedItem="{Binding SelectedType, UpdateSourceTrigger=PropertyChanged}" Width="300" Padding="8,4" Background="{StaticResource Brush.Surface}" Foreground="{StaticResource Brush.Text}" BorderThickness="1" BorderBrush="{StaticResource Brush.Border}" Margin="0,0,20,0"/>
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,6,0" Foreground="{StaticResource Brush.Text}"/>
            <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" Width="300" Padding="8,4" Background="{StaticResource Brush.Surface}" Foreground="{StaticResource Brush.Text}" BorderThickness="1" BorderBrush="{StaticResource Brush.Border}"/>
        </StackPanel>
        <DataGrid ItemsSource="{Binding FilteredItems}" AutoGenerateColumns="False" HeadersVisibility="Column"
                  CanUserAddRows="False" Background="{StaticResource Brush.Surface}" 
                  Foreground="{StaticResource Brush.Text}" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="120"/>
                <DataGridTextColumn Header="Host" Binding="{Binding Host}" Width="200"/>
                <DataGridTextColumn Header="Port" Binding="{Binding Port}" Width="80"/>
                <DataGridTextColumn Header="Ping (ms)" Binding="{Binding Ping}" Width="100"/>
                <DataGridTextColumn Header="Exit IP" Binding="{Binding ExitIp}" Width="150"/>
                <DataGridTextColumn Header="ISP" Binding="{Binding ISP}" Width="150"/>
                <DataGridTextColumn Header="City" Binding="{Binding City}" Width="120"/>
                <DataGridTextColumn Header="Country" Binding="{Binding Country}" Width="120"/>
                <DataGridTextColumn Header="Thời gian hết hạn" Binding="{Binding DaysRemaining}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
